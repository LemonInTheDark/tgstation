// Holds macros you should never use
// Go with god ~Lemon

/// This is 10% faster then if(!condition). I fucking HATE it here
/// PATENTED BY MOTHBLOCKS 2022 Method of SANIC SPEED negative-if checks
#define FAST_IFNOT_NEVER_USE(condition) if(condition){}else
